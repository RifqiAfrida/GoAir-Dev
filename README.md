Golden Rule
- folder = url
-page.tsx = halaman

✅ Next.js default SERVER
✅ API = backend
✅ File = route

1️⃣ Routing & layout
2️⃣ Login page
3️⃣ Database (Postgres + Prisma)
4️⃣ Auth
5️⃣ CRUD
6️⃣ Role
7️⃣ Dynamic form

npm install pg bcrypt jsonwebtoken cookie mapbox-gl react-leaflet leaflet
npm install -D @types/pg @types/bcrypt @types/jsonwebtoken

npm install swiper

Page = Halaman
Style ada di folder style, setiap halaman berbeda file nya.

Maps = npm install leaflet react-leaflet
npm install --save-dev @types/leaflet